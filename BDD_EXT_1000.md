### 场景 #1000 · 401 未授权触发登录刷新
Given 用户 token 已过期
When 同步请求返回 401
Then 应用静默触发 token 刷新并重试原请求

### 场景 #1001 · 401 刷新失败跳登录页
Given token 过期且 refresh_token 也失效
When 同步请求返回 401 且刷新失败
Then 跳转到登录页并显示"登录已过期"

### 场景 #1002 · 403 禁止访问显示权限不足
Given 用户对目标笔记无权限
When 同步请求返回 403
Then 弹出"无访问权限"对话框并停止该资源同步

### 场景 #1003 · 403 团队被移除
Given 用户被踢出团队空间
When 访问团队笔记请求返回 403
Then 从侧边栏移除该团队入口并提示"已离开团队"

### 场景 #1004 · 404 笔记不存在
Given 笔记在远端已被删除
When 同步请求返回 404
Then 本地标记为"已删除"并从列表移除

### 场景 #1005 · 404 软删除保留本地副本
Given 笔记远端返回 404 但本地有未上传编辑
When 检测到 404
Then 本地保留副本并提示"原笔记已被删除,是否另存"

### 场景 #1006 · 429 限流指数退避
Given 服务端返回 429
When 客户端检测到限流
Then 按 1s/2s/4s/8s 指数退避重试

### 场景 #1007 · 429 带 Retry-After 头
Given 服务端 429 响应包含 Retry-After: 30
When 客户端解析到该头
Then 等待 30 秒后再重试

### 场景 #1008 · 429 批量降级
Given 连续 3 次 429
When 应用检测到被限流
Then 将批量同步大小从 50 降到 10

### 场景 #1009 · 500 服务器内部错误重试
Given 服务端返回 500
When 客户端收到响应
Then 以指数退避最多重试 3 次

### 场景 #1010 · 502 网关错误
Given 负载均衡器返回 502
When 客户端多次收到 502
Then 切换到备用接入点

### 场景 #1011 · 503 服务不可用
Given 服务端返回 503
When 客户端检测到
Then 显示"服务维护中"横幅并暂停同步

### 场景 #1012 · 504 网关超时
Given 代理返回 504
When 客户端持续收到超时
Then 提示"服务响应慢,请稍后重试"

### 场景 #1013 · 网络超时本地保留
Given 同步请求 30 秒无响应
When 客户端触发超时
Then 本地标记为"待同步"并加入队列

### 场景 #1014 · 连接被重置
Given 网络中途被重置(ECONNRESET)
When 客户端检测到断连
Then 立即重试一次,失败后加入队列

### 场景 #1015 · DNS 解析失败
Given 设备 DNS 无法解析 API 域名
When 发起同步请求
Then 显示"无法连接到服务器,请检查网络"

### 场景 #1016 · 证书错误警告
Given 服务器证书无效
When 客户端 TLS 握手失败
Then 阻止同步并显示安全警告

### 场景 #1017 · 代理服务器拒绝
Given 设备走公司代理,代理返回 407
When 客户端请求被拦截
Then 提示用户配置代理认证

### 场景 #1018 · 弱网检测降级
Given 网络 RTT 大于 3 秒
When 客户端检测到弱网
Then 关闭实时同步,改为 5 分钟定时同步

### 场景 #1019 · 离线完全不可达
Given 设备处于飞行模式
When 用户编辑笔记
Then 本地保存并在状态栏显示"离线模式"

### 场景 #1020 · 网络恢复自动续传
Given 队列中有 8 个离线编辑
When 网络恢复
Then 自动按顺序上传并逐个显示"已同步"

### 场景 #1021 · SQLite 数据库损坏
Given 本地数据库文件损坏
When 应用启动读取数据库
Then 弹出"数据库损坏,尝试恢复"并启动修复流程

### 场景 #1022 · 数据库修复成功
Given 损坏数据库可部分恢复
When 用户确认修复
Then 抢救出可读条目并重建索引

### 场景 #1023 · 数据库修复失败回退云端
Given 本地数据库完全无法修复
When 修复流程失败
Then 提示用户从云端全量恢复

### 场景 #1024 · 迁移脚本失败回滚
Given 应用升级后迁移脚本抛异常
When 迁移中断
Then 自动回滚到旧版本 schema 并报错

### 场景 #1025 · 迁移版本跳跃
Given 用户跳过了 3 个版本
When 迁移需逐级执行
Then 按序执行每一步迁移,任一失败全部回滚

### 场景 #1026 · 迁移耗时过长
Given 本地数据 5 万条,迁移需 2 分钟
When 用户打开应用
Then 显示"正在升级数据"进度条,不可中断

### 场景 #1027 · 磁盘写满
Given 设备剩余空间不足 10MB
When 数据库写入操作触发
Then 弹窗提示"存储空间不足,请清理"

### 场景 #1028 · 磁盘写满时暂停同步
Given 写入失败提示 ENOSPC
When 客户端捕获到
Then 暂停下载和写入,保留未完成事务

### 场景 #1029 · 数据库只读模式降级
Given 磁盘压力过大
When 写入多次失败
Then 进入只读模式并提示"只读:无法新建笔记"

### 场景 #1030 · WAL 文件损坏
Given WAL 日志被外部删除
When 数据库重启
Then 重建 WAL 并警告"最近的编辑可能丢失"

### 场景 #1031 · 数据库锁超时
Given 其他进程持有数据库锁
When 当前写操作等待 10 秒
Then 放弃并重试,超过 3 次提示用户

### 场景 #1032 · 外键约束冲突
Given 关联笔记 ID 已删除
When 插入附件记录
Then 捕获外键异常并清理孤儿数据

### 场景 #1033 · 数据库 schema 不匹配
Given 旧客户端打开新格式数据库
When 读取表结构不一致
Then 阻止启动并提示"请升级应用"

### 场景 #1034 · 备份恢复成功
Given 用户启用自动备份
When 数据库损坏后触发恢复
Then 从 24 小时前的备份恢复并提示丢失的时间范围

### 场景 #1035 · 备份恢复失败
Given 备份文件也已损坏
When 恢复流程失败
Then 提示"备份不可用,请从云端同步"

### 场景 #1036 · 文件系统只读
Given 设备分区被挂载为只读
When 应用尝试写入笔记附件
Then 提示"存储不可写,请重启设备"

### 场景 #1037 · 权限拒绝存储
Given 应用未获得存储权限
When 用户尝试保存附件
Then 弹出系统权限请求或跳转设置

### 场景 #1038 · 权限永久拒绝
Given 用户勾选"不再询问"并拒绝
When 应用再次请求权限
Then 显示"请去设置手动开启"并提供跳转按钮

### 场景 #1039 · 路径不存在
Given 目标目录被用户删除
When 应用写入该路径
Then 自动创建目录并重试

### 场景 #1040 · 路径创建失败
Given 上级目录权限不足
When 自动创建失败
Then 改用应用沙盒路径并记录日志

### 场景 #1041 · 外置 SD 卡被拔出
Given 笔记数据在 SD 卡上
When 用户拔出卡
Then 显示"存储已移除,只读模式"

### 场景 #1042 · 文件被其他应用占用
Given 附件正在被其他应用读取
When 应用尝试删除
Then 提示"文件正在使用,稍后重试"

### 场景 #1043 · 文件名非法字符
Given 同步来的附件名含保留字符
When 写入本地
Then 自动替换为安全字符并记录映射

### 场景 #1044 · 超长路径
Given 合并后路径超过系统限制
When 应用尝试保存
Then 截断或哈希命名并保留原名到元数据

### 场景 #1045 · 符号链接失效
Given 外部链接目标被删除
When 读取附件
Then 提示"附件已失效"并允许重新选择源

### 场景 #1046 · SSO 会话缺失
Given SSO 会话已过期
When 用户访问团队空间
Then 重新跳转 SSO 登录流程

### 场景 #1047 · SSO 配置缺失
Given 管理员未启用 SSO
When 用户选择 SSO 登录
Then 提示"未配置 SSO,请联系管理员"

### 场景 #1048 · OAuth token 过期
Given 第三方集成 token 过期
When 用户触发集成操作
Then 自动刷新或引导重新授权

### 场景 #1049 · OAuth 授权被撤销
Given 用户在第三方平台撤销授权
When 同步该集成数据
Then 提示"授权已失效,请重新连接"

### 场景 #1050 · 权限降级提示
Given 用户角色从 admin 降为 viewer
When 访问原本可编辑的笔记
Then 切换为只读模式并提示"您的权限已变更"

### 场景 #1051 · 二步验证缺失
Given 账户开启 2FA 但设备未通过
When 访问敏感操作
Then 弹出 2FA 验证页面

### 场景 #1052 · 会话被管理员踢出
Given 管理员强制下线该账户
When 客户端下次请求
Then 接收到特殊错误码并跳登录页

### 场景 #1053 · IP 限制拒绝
Given 企业策略限制 IP 白名单
When 设备 IP 不在白名单
Then 显示"网络环境受限,请连接公司 VPN"

### 场景 #1054 · 设备数量超限
Given 账户设备数已达上限
When 新设备登录
Then 提示"设备数超限,请踢出其他设备"

### 场景 #1055 · 订阅到期功能锁定
Given 付费订阅已到期
When 使用付费功能
Then 弹出"订阅已到期,请续费"并禁用功能

### 场景 #1056 · 写入时进程被杀
Given 用户正在编辑笔记
When 系统低内存杀掉应用
Then 下次启动恢复草稿并提示"检测到未保存更改"

### 场景 #1057 · OOM 导致崩溃
Given 应用加载巨大附件溢出内存
When 进程被系统强杀
Then 重启后跳过该附件并记录错误

### 场景 #1058 · 崩溃后自动上报
Given 应用崩溃产生 stacktrace
When 重启应用
Then 静默上传崩溃日志(用户同意过)

### 场景 #1059 · 崩溃循环检测
Given 应用连续崩溃 3 次
When 第 4 次启动
Then 进入安全模式仅加载本地数据

### 场景 #1060 · 安全模式
Given 应用处于安全模式
When 用户进入主界面
Then 仅显示笔记列表,关闭同步和插件

### 场景 #1061 · 后台写入被中断
Given 后台正在保存大笔记
When 系统回收后台进程
Then 下次启动从检查点续写

### 场景 #1062 · ANR 恢复
Given 主线程阻塞 5 秒以上
When 系统触发 ANR
Then 应用记录堆栈并自我恢复

### 场景 #1063 · 崩溃前未保存提示
Given 崩溃后检测到未保存草稿
When 用户打开同名笔记
Then 顶部显示"恢复未保存版本"按钮

### 场景 #1064 · 低内存预警
Given 系统发出低内存信号
When 应用接收到
Then 释放图片缓存和不活跃 tab

### 场景 #1065 · 写入磁盘事务保护
Given 正在提交数据库事务
When 进程被强杀
Then WAL 机制保证下次启动数据一致

### 场景 #1066 · 冲突-本地覆盖
Given 同一笔记本地和远端都有修改
When 用户选择"保留本地"
Then 上传本地版本并覆盖远端

### 场景 #1067 · 冲突-远端覆盖
Given 同一笔记本地和远端都有修改
When 用户选择"保留远端"
Then 拉取远端版本并丢弃本地改动

### 场景 #1068 · 冲突-手动合并
Given 双方都有不同段落修改
When 用户选择"手动合并"
Then 打开三栏 diff 编辑器供编辑

### 场景 #1069 · 冲突-创建副本
Given 用户不愿丢失任一版本
When 选择"保留两者"
Then 本地版本另存为"笔记名(冲突副本)"

### 场景 #1070 · 冲突-取消保留待定
Given 冲突对话框打开
When 用户点击取消
Then 维持本地为"冲突待解决"状态,下次提示

### 场景 #1071 · 冲突批量处理
Given 同步中产生 10 个冲突
When 用户选择"全部应用本地"
Then 批量以本地优先解决所有冲突

### 场景 #1072 · 冲突三方合并基准
Given 客户端保留共同祖先版本
When 执行自动合并
Then 基于共同祖先做三方合并减少冲突

### 场景 #1073 · 冲突标记持久化
Given 冲突未解决
When 用户关闭应用
Then 重启后冲突依然存在待处理

### 场景 #1074 · 指数退避上限
Given 连续失败导致退避时间延长
When 退避时间达到 5 分钟
Then 不再翻倍,固定为 5 分钟

### 场景 #1075 · 重试抖动
Given 多个客户端同时重试
When 计算下次重试时间
Then 加入 ±30% 随机抖动避免雪崩

### 场景 #1076 · 手动重试按钮
Given 操作失败显示"失败"标记
When 用户点击"重试"
Then 立即重试并重置退避计数

### 场景 #1077 · 自动重试次数上限
Given 一个操作持续失败
When 重试达 5 次
Then 停止自动重试并等待用户手动触发

### 场景 #1078 · 重试状态可见
Given 后台正在重试
When 用户查看状态栏
Then 显示"重试中(第 2 次,8 秒后)"

### 场景 #1079 · 断点续传
Given 大附件上传到 60% 断网
When 网络恢复
Then 从 60% 继续上传而非重新开始

### 场景 #1080 · 失败队列持久化
Given 有 3 个失败操作
When 用户关闭应用
Then 重启后队列依然存在继续重试

### 场景 #1081 · 重试优先级
Given 队列中有文本和附件操作
When 恢复同步
Then 优先重试小文本,后重试大附件

### 场景 #1082 · 死信队列
Given 操作重试超过 10 次
When 最终失败
Then 移入死信队列并通知用户查看

### 场景 #1083 · 回退到缓存
Given 远端请求失败
When 本地有缓存
Then 使用缓存数据并标注"离线数据"

### 场景 #1084 · snackbar 轻提示
Given 网络短暂抖动导致同步失败
When 自动重试成功
Then 显示 snackbar"已同步"2 秒自动消失

### 场景 #1085 · dialog 阻塞提示
Given 需要用户做出决策的冲突
When 检测到冲突
Then 弹出 modal dialog 必须选择才能继续

### 场景 #1086 · inline 错误提示
Given 单条笔记同步失败
When 用户查看笔记列表
Then 该条目右上角显示红色感叹号

### 场景 #1087 · 顶部横幅错误
Given 全局同步故障
When 应用检测到
Then 顶部显示红色横幅"同步已停止"并附重试按钮

### 场景 #1088 · 错误图标状态
Given 状态栏图标显示同步状态
When 失败时
Then 图标变红色并叠加感叹号

### 场景 #1089 · 错误通知权限
Given 应用需发系统通知
When 没有通知权限
Then 首次请求权限,被拒后仅内部提示

### 场景 #1090 · 错误提示本地化
Given 用户语言为中文
When 显示错误
Then 使用中文错误文案,不显示原始 error code

### 场景 #1091 · 错误日志级别
Given 应用记录日志
When 发生错误
Then 按 INFO/WARN/ERROR 分级并带时间戳

### 场景 #1092 · 日志自动轮转
Given 日志文件超过 10MB
When 下次写入
Then 归档旧文件并新建,最多保留 7 天

### 场景 #1093 · 诊断模式开关
Given 用户联系客服
When 开启诊断模式
Then 记录详细请求响应,24 小时后自动关闭

### 场景 #1094 · 敏感信息脱敏
Given 日志包含 token 和邮箱
When 写入日志
Then 自动脱敏为 tok_*** 和 u***@example.com

### 场景 #1095 · 导出日志给客服
Given 用户遇到问题
When 点击"导出日志"
Then 生成 zip 文件供分享,包含最近 3 天日志

### 场景 #1096 · 错误引导去设置
Given 错误源于权限未开
When 显示错误对话框
Then 提供"去设置"按钮直接跳转系统设置页

### 场景 #1097 · 错误引导重试
Given 错误为临时网络问题
When 显示错误提示
Then 提供"重试"按钮,点击后立即重试

### 场景 #1098 · 错误引导取消
Given 错误为用户可放弃的操作
When 显示错误
Then 提供"取消"按钮,点击后清除该操作

### 场景 #1099 · 错误引导联系客服
Given 错误反复发生无法自愈
When 用户查看错误详情
Then 显示"联系客服"按钮并预填错误 ID
