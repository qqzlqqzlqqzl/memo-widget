<?xml version="1.0" encoding="utf-8"?>
<!--
    FileProvider paths — only exposes cacheDir/logs/, the directory
    LogExporter writes into. No other internal storage is shared.
-->
<paths>
    <cache-path name="logs" path="logs/" />
</paths>
