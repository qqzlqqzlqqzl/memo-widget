### 场景 #1100 · 冷启动首屏 2 秒内可交互
Given 应用已被系统完全杀死，设备为中端机型
When 用户从桌面图标启动应用
Then 首页在 2 秒内完成渲染并响应用户点击

### 场景 #1101 · 冷启动闪屏时长控制
Given 应用处于冷启动状态
When 闪屏页面开始显示
Then 闪屏最多停留 800 毫秒后自动进入主界面

### 场景 #1102 · 冷启动数据库延迟初始化
Given 应用冷启动
When 启动流程开始执行
Then 数据库仅在首次访问时初始化，不阻塞启动

### 场景 #1103 · 冷启动 Application.onCreate 耗时
Given 应用冷启动
When Application 的 onCreate 方法被调用
Then onCreate 执行时间不超过 300 毫秒

### 场景 #1104 · 冷启动首帧绘制时间
Given 应用被冷启动
When 主 Activity 开始渲染首帧
Then 首帧绘制在启动后 1200 毫秒内完成

### 场景 #1105 · 冷启动字体预加载优化
Given 应用冷启动
When 启动流程需要加载自定义字体
Then 字体采用异步加载，不阻塞主线程

### 场景 #1106 · 冷启动低端机型降级策略
Given 设备为低端机型（内存 2GB）
When 用户冷启动应用
Then 应用在 3 秒内到达可交互状态，并跳过非必要动画

### 场景 #1107 · 冷启动关键路径任务分级
Given 应用冷启动过程中存在多个初始化任务
When 启动流程开始
Then 仅执行关键路径任务，次要任务延迟到空闲期执行

### 场景 #1108 · 冷启动用户感知时间
Given 用户点击应用图标
When 从点击到首屏出现
Then 用户感知的冷启动时间不超过 2 秒

### 场景 #1109 · 冷启动资源文件懒加载
Given 应用冷启动
When 启动过程需要加载资源
Then 图片、图标等资源按需加载，不一次性全部载入

### 场景 #1110 · 热启动 500 毫秒内恢复
Given 应用已在后台停留 5 分钟
When 用户重新打开应用
Then 应用在 500 毫秒内恢复到上次界面

### 场景 #1111 · 热启动内存状态保持
Given 应用曾经运行过，进程仍在后台
When 用户再次进入应用
Then 内存中的数据状态完整保留，无需重新加载

### 场景 #1112 · 热启动主界面无白屏
Given 应用处于热启动场景
When 用户从桌面返回应用
Then 不出现白屏或加载转圈

### 场景 #1113 · 热启动触发背景数据刷新
Given 应用热启动且距上次使用超过 1 分钟
When 用户进入首页
Then 触发后台数据刷新，但不阻塞 UI

### 场景 #1114 · 热启动恢复滚动位置
Given 用户在列表中滚动到中间位置后切出应用
When 用户热启动应用
Then 列表恢复到离开时的滚动位置

### 场景 #1115 · 热启动动画简化
Given 应用正在热启动
When 主界面开始显示
Then 省略开屏动画，直接展示内容

### 场景 #1116 · 热启动耗时监控上报
Given 应用发生热启动
When 启动完成
Then 记录启动耗时并在下次冷启动时上报

### 场景 #1117 · 热启动并发任务限制
Given 应用在热启动过程中
When 系统资源紧张
Then 限制并发任务数量，优先保证 UI 响应

### 场景 #1118 · 热启动 widget 同步触发
Given 应用热启动完成
When 主界面可见
Then 自动向桌面 widget 发送数据刷新请求

### 场景 #1119 · 热启动低内存场景降级
Given 系统报告内存紧张
When 应用被热启动
Then 跳过耗资源的预加载操作

### 场景 #1120 · 列表滚动保持 60 FPS
Given 用户在笔记列表页
When 用户快速滚动列表
Then 滚动期间帧率稳定在 60 FPS 以上

### 场景 #1121 · 列表滚动丢帧率低于 1%
Given 用户在长列表中滚动
When 统计滚动期间的丢帧
Then 丢帧率控制在 1% 以下

### 场景 #1122 · 列表 item 视图复用
Given 列表包含 500 条笔记
When 用户滚动列表
Then RecyclerView 复用已有视图，不重复创建

### 场景 #1123 · 列表图片异步加载
Given 列表 item 包含缩略图
When 用户快速滚动
Then 图片异步加载，不阻塞滚动

### 场景 #1124 · 列表滚动图片取消机制
Given 用户快速滚动经过多张图片
When item 滚出屏幕
Then 该 item 的图片加载请求被取消

### 场景 #1125 · 列表滚动页面缓存
Given 列表支持分页加载
When 用户向下滚动接近底部
Then 提前预加载下一页数据，避免加载停顿

### 场景 #1126 · 列表滚动 overdraw 优化
Given 列表 item 视图结构复杂
When 用户滚动
Then overdraw 层数不超过 2.5x

### 场景 #1127 · 列表滚动 GC 抑制
Given 用户持续滚动 10 秒以上
When 统计 GC 次数
Then 滚动期间 Major GC 不超过 1 次

### 场景 #1128 · 列表快速滑动 fling 流畅度
Given 用户快速 fling 列表
When fling 过程结束
Then 整个动画无卡顿，减速自然

### 场景 #1129 · 列表项动画不影响滚动
Given 列表 item 支持添加删除动画
When 用户正在滚动时触发动画
Then 动画不降低滚动 FPS

### 场景 #1130 · 1000 条笔记加载时间
Given 用户账户中有 1000 条笔记
When 用户打开笔记列表
Then 首屏数据在 500 毫秒内展示

### 场景 #1131 · 10000 条笔记分页加载
Given 用户账户中有 10000 条笔记
When 用户打开列表
Then 采用分页加载，每页 50 条，不一次性加载全部

### 场景 #1132 · 1000 条笔记搜索耗时
Given 用户有 1000 条笔记
When 用户输入关键词搜索
Then 搜索结果在 300 毫秒内返回

### 场景 #1133 · 10000 条笔记搜索耗时
Given 用户有 10000 条笔记
When 用户输入关键词搜索
Then 搜索结果在 1 秒内返回

### 场景 #1134 · 10000 条笔记内存占用
Given 用户有 10000 条笔记
When 用户浏览列表
Then 应用内存占用不超过 200MB

### 场景 #1135 · 10000 条笔记标签筛选
Given 用户有 10000 条笔记，使用了 100 个标签
When 用户按标签筛选
Then 筛选结果在 500 毫秒内显示

### 场景 #1136 · 1000 条笔记排序切换
Given 用户有 1000 条笔记
When 用户切换排序方式
Then 列表在 200 毫秒内重新排序并显示

### 场景 #1137 · 10000 条笔记导出耗时
Given 用户有 10000 条笔记
When 用户触发全量导出
Then 导出操作在后台完成，不阻塞 UI

### 场景 #1138 · 大数据集初始化数据库
Given 首次同步 10000 条笔记到本地
When 数据库执行批量插入
Then 使用事务批处理，插入耗时不超过 10 秒

### 场景 #1139 · 大数据集列表滚动流畅度
Given 列表中有 10000 条笔记
When 用户从头滚到底
Then 整个过程 FPS 不低于 55

### 场景 #1140 · 大数据集 widget 预览
Given 用户有 10000 条笔记
When widget 刷新显示最近笔记
Then widget 仅查询最近 20 条，不加载全部

### 场景 #1141 · 大数据集备份耗时
Given 用户有 10000 条笔记
When 用户触发本地备份
Then 备份文件在 30 秒内生成

### 场景 #1142 · 大数据集恢复耗时
Given 备份文件包含 10000 条笔记
When 用户触发恢复
Then 恢复操作在 60 秒内完成

### 场景 #1143 · 1000 条笔记批量操作
Given 用户选中 1000 条笔记
When 用户执行批量删除
Then 操作在 3 秒内完成并更新 UI

### 场景 #1144 · 大数据集分页预加载策略
Given 用户有 5000 条笔记
When 用户滚动到第 80% 位置
Then 自动加载下一页，保证无感切换

### 场景 #1145 · widget 内存占用低于 15MB
Given 桌面 widget 正在运行
When 系统统计 widget 内存
Then widget 进程内存占用不超过 15MB

### 场景 #1146 · app 前台运行内存占用
Given 应用处于前台正常使用状态
When 用户浏览笔记
Then app 内存占用稳定在 150MB 以下

### 场景 #1147 · app 后台运行内存占用
Given 应用切换到后台
When 后台驻留超过 5 分钟
Then app 内存占用降至 50MB 以下

### 场景 #1148 · worker 进程内存占用
Given 后台同步 worker 正在运行
When 同步任务执行中
Then worker 进程内存占用不超过 30MB

### 场景 #1149 · 内存泄漏检测
Given 用户多次进入退出笔记详情页
When 反复操作 50 次后
Then 应用无内存泄漏，内存占用保持稳定

### 场景 #1150 · 内存压力下缓存释放
Given 系统报告低内存（TRIM_MEMORY_RUNNING_LOW）
When 应用收到内存警告
Then 主动释放图片缓存和非必要数据

### 场景 #1151 · 大图加载内存优化
Given 用户打开包含大图的笔记
When 图片加载到内存
Then 根据显示尺寸采样，避免加载原始大图

### 场景 #1152 · Bitmap 复用机制
Given 列表中包含多张相似尺寸图片
When 用户滚动时加载图片
Then 启用 BitmapPool 复用已回收的 Bitmap

### 场景 #1153 · 内存 dump 自动触发
Given 应用内存占用超过阈值 300MB
When 检测到异常高内存
Then 自动生成内存 dump 并异步上报

### 场景 #1154 · 进程间内存隔离
Given widget 和 app 是独立进程
When app 内存溢出崩溃
Then widget 进程不受影响，继续显示

### 场景 #1155 · 后台同步电量消耗
Given 应用启用后台同步功能
When 设备充电状态外运行 1 小时
Then 应用电量消耗不超过总电量的 3%

### 场景 #1156 · 轮询改为长连接省电
Given 应用需要监听数据更新
When 选择通信方式
Then 优先使用长连接而非高频轮询

### 场景 #1157 · 使用 JobScheduler 批量任务
Given 应用有多个后台同步任务
When 任务调度
Then 使用 JobScheduler 合并任务，减少唤醒次数

### 场景 #1158 · 低电量模式降级
Given 设备电量低于 15%，进入省电模式
When 应用检测到省电模式
Then 关闭后台同步，延迟非必要任务

### 场景 #1159 · Doze 模式兼容
Given 设备进入 Doze 模式
When 后台任务被限制
Then 应用遵守 Doze 约束，不尝试唤醒设备

### 场景 #1160 · 唤醒锁使用审计
Given 应用使用 WakeLock
When 统计 WakeLock 持有时间
Then 所有 WakeLock 持有时间总和不超过运行时间的 5%

### 场景 #1161 · GPS 定位按需使用
Given 笔记支持位置标记（可选）
When 用户未启用位置功能
Then 应用不请求 GPS，不消耗定位电量

### 场景 #1162 · 动画在省电模式下禁用
Given 设备处于省电模式
When 应用检测到该状态
Then 禁用界面动画，减少 GPU 消耗

### 场景 #1163 · 增量同步流量优化
Given 用户本地有 1000 条笔记
When 应用执行同步
Then 仅上传下载变更部分，流量不超过 50KB

### 场景 #1164 · 全量同步触发条件
Given 本地数据可能与服务器不一致
When 用户手动触发全量同步
Then 全量同步执行，其他时候默认增量同步

### 场景 #1165 · 同步请求合并
Given 短时间内用户修改多条笔记
When 触发同步
Then 批量合并请求，不发送多个独立请求

### 场景 #1166 · 图片上传压缩
Given 用户上传笔记中的图片
When 图片大于 1MB
Then 客户端先压缩至 1MB 以下再上传

### 场景 #1167 · 响应数据 gzip 压缩
Given 服务器返回笔记列表
When 客户端请求
Then 响应数据使用 gzip 压缩，减少流量 70%

### 场景 #1168 · 弱网环境流量控制
Given 用户处于 2G 网络环境
When 应用检测到弱网
Then 降低图片质量，跳过非必要请求

### 场景 #1169 · 后台自动同步 WiFi 限制
Given 应用默认在移动网络下不自动同步
When 用户在移动网络中
Then 仅在 WiFi 下触发自动同步

### 场景 #1170 · 流量统计页面
Given 用户关心流量消耗
When 用户查看流量统计
Then 应用展示最近 7 天上传下载流量详情

### 场景 #1171 · 存储空间增长预估
Given 用户使用应用 3 个月
When 统计存储占用
Then 平均每月增长不超过 50MB

### 场景 #1172 · 数据库文件压缩
Given 数据库存在大量删除的历史记录
When 触发数据库清理
Then 执行 VACUUM，回收磁盘空间

### 场景 #1173 · 图片缓存大小限制
Given 图片缓存目录
When 缓存达到 100MB 上限
Then 采用 LRU 策略淘汰旧缓存

### 场景 #1174 · 日志文件大小控制
Given 应用持续输出日志
When 单个日志文件超过 5MB
Then 自动切分新文件，保留最多 3 个历史文件

### 场景 #1175 · 存储低于阈值告警
Given 设备可用存储低于 500MB
When 应用启动
Then 提示用户清理，并限制新增笔记附件

### 场景 #1176 · 主线程零 IO 原则
Given 应用正在运行
When 任意主线程操作执行
Then 无文件读写或网络请求在主线程进行

### 场景 #1177 · 数据库查询移至子线程
Given 用户触发笔记查询
When 查询执行
Then 所有数据库操作在子线程完成，主线程只更新 UI

### 场景 #1178 · SharedPreferences 异步提交
Given 应用需要写入配置
When 使用 SharedPreferences
Then 使用 apply 而非 commit，避免主线程阻塞

### 场景 #1179 · 图片解码异步处理
Given 界面需要显示图片
When 图片需要解码
Then BitmapFactory.decode 在子线程执行

### 场景 #1180 · ContentProvider 查询异步化
Given 应用查询系统 ContentProvider
When 主线程发起查询
Then 使用 CursorLoader 或协程切换到 IO 线程

### 场景 #1181 · 网络请求必须异步
Given 应用调用网络 API
When 请求发起
Then 所有请求通过异步方式，主线程不直接 block

### 场景 #1182 · 文件写入异步化
Given 应用保存笔记到本地文件
When 触发保存
Then 文件写入在 IO 线程，UI 立即反馈成功

### 场景 #1183 · 主线程耗时监控
Given 应用主线程可能执行耗时操作
When 单次操作耗时超过 200 毫秒
Then 触发警告并记录堆栈，便于排查

### 场景 #1184 · StrictMode 开启拦截违规
Given 应用为 debug 构建
When 启动时初始化
Then 启用 StrictMode，拦截主线程 IO 违规

### 场景 #1185 · ANR 阈值预警
Given 主线程某操作接近 ANR 阈值
When 操作耗时超过 3 秒
Then 提前中断操作并在下次启动上报

### 场景 #1186 · 笔记 ID 字段索引
Given 数据库表包含 note_id 字段
When 执行按 ID 查询
Then SQL 执行计划显示命中 note_id 索引

### 场景 #1187 · 更新时间字段索引
Given 列表按更新时间排序
When 执行 ORDER BY updated_at DESC
Then 查询使用 updated_at 索引，避免全表扫描

### 场景 #1188 · 全文搜索 FTS 索引
Given 用户进行全文搜索
When 搜索关键词
Then 使用 FTS4/FTS5 虚拟表，查询速度优于 LIKE

### 场景 #1189 · 复合索引优化
Given 查询同时过滤用户 ID 和状态
When 执行查询
Then 存在 (user_id, status) 复合索引并被命中

### 场景 #1190 · 索引命中率监控
Given 应用执行数据库查询
When 定期抽样分析
Then 90% 以上查询命中索引，不触发全表扫描

### 场景 #1191 · 慢查询日志记录
Given 某 SQL 查询超过 100 毫秒
When 查询完成
Then 慢查询 SQL 和执行计划被记录到本地日志

### 场景 #1192 · 索引自动重建
Given 数据库在升级后索引可能失效
When 应用首次启动新版本
Then 自动检测并重建必要的索引

### 场景 #1193 · widget 渲染耗时低于 100 毫秒
Given 桌面 widget 被刷新
When 系统触发 onUpdate
Then widget 渲染完成耗时不超过 100 毫秒

### 场景 #1194 · widget 数据预取
Given widget 即将刷新
When 刷新前 1 秒
Then 后台服务已将数据预取到内存，widget 直接读取

### 场景 #1195 · widget 避免主线程渲染
Given widget 刷新需要从数据库读取
When RemoteViewsFactory 被调用
Then 数据读取在后台线程，RemoteViews 构建快速返回

### 场景 #1196 · widget 图片使用缓存
Given widget 显示笔记缩略图
When widget 刷新
Then 图片从缓存读取，不重新解码

### 场景 #1197 · widget 批量更新防抖
Given 短时间内多次请求刷新 widget
When 1 秒内触发 5 次刷新
Then 合并为单次刷新，避免渲染过载

### 场景 #1198 · widget 渲染错误降级
Given widget 渲染过程中遇到异常
When 数据格式错误
Then widget 显示默认占位内容，不崩溃

### 场景 #1199 · widget 渲染性能上报
Given widget 每次刷新
When 渲染完成
Then 记录耗时指标，异常值自动上报便于优化
